//normal way to calculate the area 
function calculateArea(width, height) {
    return width * height;
}
//another way to calculate the area 
console.log(calculateArea(5, 6));
let calculateAreaarrow = (width, height) => width * height; //this function is known as arrow function
console.log(calculateAreaarrow(5, 6));
export {};

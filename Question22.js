let friends = ["Umar", "Areeb", "Abdullah", "Moosa", "Najam"];
//creating an intentional error by acessing the index which is not present in the array
console.log(friends[5]); //[5] this mean index of array which starts from 0 so 5 means the 6th element which 
//isnt present in the array
console.log(friends[4]); //this will print the last element in the array 
export {};

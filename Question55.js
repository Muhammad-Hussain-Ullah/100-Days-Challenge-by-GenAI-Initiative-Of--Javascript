let number = [1, 2, 3, 4, 5];
//to make the numbers double in number array
let doubleNumber = number.map(number => number * 2);
console.log(`Orignal number array ${number}`);
console.log((`Doubles of above array ${doubleNumber}`));
export {};
